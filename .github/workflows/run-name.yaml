name: Run Name Example
run-name: ${{github.ref_name + " " + github.event_name + " " + github_actor}} 
on:
    push:
        branches:
            - main

jobs:
    test:
        runs-on: ubuntu-latest
        steps:
            - name: print hell world
              run: echo "Hello World"